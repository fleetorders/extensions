{
  "name": "gephyra ↔ claude-code",
  "_note": "gephyra's deeper risk is the model-slot remap and ANTHROPIC_* env contract; a dedicated 'model-slot-resolves' / 'env-contract' probe is a future agent-doctor probe type. These probes cover the harness it drives. Until a script probe type exists, the vision-grounding check runs beside this manifest: scripts/vision-probe.sh <profile> at every provider or model version change (exit 0 grounded, 1 ungrounded — arm gephyra.visionProxy, 2 probe error).",
  "harness": "claude",
  "versionArgs": ["--version"],
  "probes": [
    { "type": "command-exists", "critical": true },
    { "type": "version" }
  ]
}
