{
  "gates": {
    "commitFormat": true,
    "_why": {
      "commitFormat": "This repo's history was rewritten to Conventional Commits, so it asks for the door that keeps it that way. The check is off by default in the pack — it is an opinion, and a repo has to choose it."
    }
  }
}
