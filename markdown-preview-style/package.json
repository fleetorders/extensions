{
  "name": "markdown-preview-style",
  "displayName": "Markdown Preview Style",
  "description": "A markdown-preview reading stylesheet, contributed globally so every workspace gets it with no per-repo settings.",
  "version": "1.1.0",
  "publisher": "alkisyuv",
  "license": "MIT",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "keywords": [
    "markdown",
    "preview",
    "css"
  ],
  "contributes": {
    "markdown.previewStyles": [
      "./preview.css",
      "./local-override.css"
    ],
    "configuration": {
      "title": "Markdown Preview Style",
      "properties": {
        "markdownPreviewStyle.localOverride": {
          "type": "string",
          "default": "~/.config/markdown-preview-style/override.css",
          "description": "Machine-local CSS file layered over the bundled stylesheet. Synced into the extension on activation and on change (a window reload applies it). Empty string disables the override."
        }
      }
    }
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/triartleet/extensions/tree/main/markdown-preview-style"
  },
  "icon": "media/icon.png",
  "main": "./extension.js",
  "activationEvents": [
    "onStartupFinished"
  ]
}
